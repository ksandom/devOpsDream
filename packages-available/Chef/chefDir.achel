# Set the directory where your chef config is located. You only need to run this once. --chefDir=/path/to/chef/config ~ chef,config
parameters dir

assertChef

setNested Chef,config,dir,~!Local,dir!~
