# Assert that chef is properly configured. ~ chef,config,hidden

collectionLoad Chef

if ~!Chef,config,dir!~,==,,
	debug 0,Chef directory is not configured. You can do so with --chefDir=/path/to/chef/config

