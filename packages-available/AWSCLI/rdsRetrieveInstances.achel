# Retrieve information about all RDS instances ~ aws,redshift
parameters {"region":"eu-west-1"}

setIfNotSet Profile,name,default
exExec aws rds describe-db-instances --output json --region ~!Local,region!~ --profile ~!Profile,name!~

setCategory RDSTmp
retrieveResults RDSTmp,DBInstances
unsetCategory RDSTmp
resultSet Profile,~!Profile,name!~
